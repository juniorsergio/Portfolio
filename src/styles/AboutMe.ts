import styled from "styled-components";

export const Container = styled.article`
    flex: auto;
    align-items: center;

    figure {
        display: flex;
        justify-content: center;

        img {
            max-width: 80%;
        }
    }
`
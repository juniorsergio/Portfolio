import styled from "styled-components";

export const Container = styled.article`
    flex: auto;
    align-items: center;

    figure {
        display: flex;
        justify-content: center;

        img {
            width: 90%;
            max-width: 500px;
        }
    }
`